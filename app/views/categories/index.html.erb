<section class='container'>
  <div class="text-end">
    <%= link_to 'Add Category', new_category_path, class: 'btn btn-primary' %>
  </div>
  <h1>Categories#index</h1>
  <ul>
    <% @categories.each do |category| %>
      <li><%= link_to category.name, category_expenses_path(category) %></li>
    <% end %>
  </ul>
</section>
